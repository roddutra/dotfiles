# Round <N> brief (concepts <IDS>)

<!-- Template for briefing designer agents after a panel. Fill every <PLACEHOLDER>, delete sections that
don't apply, and keep the whole brief under ~150 lines: designers follow short, specific briefs better. -->

## Why this round exists
<One paragraph: what the panel found (winners, why, what was missing) and any new decisions since.>

## Read first (in this order)
1. This file.
2. `<decisions file>`: binding decisions made since the panel (scope, pricing, audience).
3. `<synthesis file>`: read all of it; sections <X, Y> matter most.
4. The code and screenshots of the winning concepts, plus the specific blocks to borrow named in the synthesis.
5. `<product public-language or brand guide, if any>`.

## Who is reading
<The target reader in one list: roles, what they already know, how they read (skim, phone, forward).>

## What every concept must do
<!-- Numbered, one requirement per item, each traceable to a panel finding. Typical items: -->
1. **Scope is plain** early on (who it's for), plus how out-of-scope readers are told "not yet".
2. **Keep the winners' spine**: <the headline pattern, hero ticks, problem-in-their-words, setup steps>.
3. **Carry the proof into the short format**: <the proof blocks that won in losing concepts>, each used once, small and legible.
4. **Answer the deciding questions** from the panel's "questions a page must answer" list, visibly and not collapsed.
5. **Treat every mockup line as real copy** a regulator, customer or reviewer will read literally. Figures agree across every surface.
6. **Kill list**: <what lost on every model: e.g. chatbot hero, mascots, dark consoles, long multi-chapter stories, jargon>.
7. **Fresh cast per concept.** New demo names and places per concept; none shared with another concept or matching a panel persona.
8. **No build notes or placeholder labels in the page.** Anything a reader sees, they will judge.

## Structure and length
- Allowed pages: <core pages only>. Home must stand alone.
- Length targets: <e.g. home 6,000 to 9,000px desktop, under 11,000px at 390 wide>. The last panel penalised length.

## The concepts
<!-- Make them differ in structure, not message, so the next panel measures structure. -->
- **<id>, "<name>":** <one-paragraph definition>. Folder: `<folder>/`.

## Hard rules
- Work only in your concept folder; no commits.
- <Asset, font, CDN and screenshot-location rules.>

## Verification
- Check at desktop and phone widths; incremental scroll screenshots of every page.
- At least two critique-and-fix passes, including a reader test: would <target reader> get it in a glance and care?
- No console errors; every nav link works.
- Grep your folder for the kill list, placeholder wording and forbidden characters.

## Report (concise)
Entry file and pages, a 4 to 6 line summary, which findings each page answers, page heights, the 5-second and explain-it-back lines, deviations, rough edges.
